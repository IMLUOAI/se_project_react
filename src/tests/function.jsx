const sayHello = (firstName, lastName) => {
    return `Hello, Mr. ${firstName} ${lastName}!`;
}

const wellDoneSprint14 = (firstName, lastName) => {
    return `Congrats, passed the Sprint 14 ${firstName} ${lastName}`
}

module.exports = {
sayHello,
wellDoneSprint14,
}