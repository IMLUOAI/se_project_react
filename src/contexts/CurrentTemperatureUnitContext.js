import React from "react";

const CurrentTemperatureUnitContext = React.createContext({
    currentTemperatureUnit: "",
    handleToggleSewitchChange: () => {

    }
});

export CurrentTemperatureUnitContext;